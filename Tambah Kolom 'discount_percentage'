-- Cara lain untuk menambahkan kolom baru dengan mereplace tabel yang sudah ada, dan menambahkan kolom di bawah nya.
CREATE OR REPLACE TABLE kimia_farma.tabel_analisa as
SELECT
    ta.*,                                       -- Memilih semua data yang akan di replace dari tabel yang sudah ada
    ft.discount_percentage                      -- Untuk menambahkan kolom Presentase Diskon
FROM
    kimia_farma.tabel_analisa ta                -- Penentu sumber data utama dari mana data akan diambil
JOIN
    kimia_farma.kf_final_transaction ft         -- Kluasa JOIN yang digunakan untuk menggabungkan data dari dua tabel
ON
    ta.transaction_id = ft.transaction_id;      -- Penentu bagaimana ke 2 tabel itu dapat bergabung (Primary & Foreign)
